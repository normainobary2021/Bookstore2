const bookDetails = (detail) => {
  const { title, author, ids } = detail;
  return {
    id: ids,
    title,
    author,
    categories: category,
    authors: author,
  };
};
export default bookDetails;
